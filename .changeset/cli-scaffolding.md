---
'@canvas-commons/cli': minor
---

Add `@canvas-commons/cli`, a pnpm-first command line tool for scaffolding a
monorepo of animations (`init`), creating TypeScript projects inside it
(`project`), and adding scenes that auto-register in a project (`scene`).
